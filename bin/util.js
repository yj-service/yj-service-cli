const download = require('download-git-repo');
/**
 * 
 * @param {*} url git仓库url
 * @param {*} destination  目标位置
 * @param {*} filter  过滤器
 */
export function downloadTpl(url,destination,filter){
  
} 